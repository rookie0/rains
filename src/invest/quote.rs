#[derive(Debug, Default)]
pub struct Quote {
    pub symbol: String,
    pub name: String,
    pub now: String,
    pub close: String,
    pub open: String,
    pub high: String,
    pub low: String,
    pub buy: String,
    pub sell: String,
    pub turnover: String,
    pub volume: String,
    pub date: String,
    pub time: String,
    // pub currency: String,
}
